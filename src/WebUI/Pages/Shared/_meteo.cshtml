@using Application.Features.Location.Queries.GetMeteoInfo
@model MeteoViewModel
@{
}


<div>    
    <div>
        <img src="@Model.ImageUrl" width="50px" height="50px" alt="">
        <span>@Model.Temperature °</span>
    </div>    
    <div>
        <span>@(Model.WeatherCode != -1 ? Model.WeatherLabel : Model.Location)</span>
    </div>    
</div>